# 상시 운영 시작 · 종료 (DGX, 3페르소나 파이프라인)

현재 구성 기준 빠른 참조. 모든 명령은 `/home/jun/agent` 에서 실행.

## 현재 구성
- 실행 위치: **DGX (Linux, spark-1397)** — 사용자 Mac 아님.
- 3페르소나 동시: **방어(DGX_1) · 공격(DGX_2) · 분석가(DGX_3)** — `agents.json`.
- 모델: **gemma4:31b** (`OLLAMA_THINK=false` 필수), 계층2 파이프라인(`USE_PIPELINE=true`),
  분석 게시만(`AGENT_ANALYSIS_ONLY=true`), 사이클 간격 `AGENT_INTERVAL=400`초.
- 설정: `.env`(공통) + `agents.json`(페르소나별 토큰). 둘 다 `.gitignore` 제외.

---

> **관리 방식: systemd user 서비스 `kestrel-agent`** 로 운영합니다(재부팅/크래시 자동 재시작).
> 이제 `agentctl.sh start` 는 쓰지 마세요 — systemd 가 관리하는 프로세스와 이중 실행됩니다.

## 전제: Ollama 서버가 떠 있어야 함
```bash
curl -s http://localhost:11434/api/version   # {"version":...} 나오면 정상
# 안 떠 있으면:  ollama serve &
```

## 시작 / 종료 / 재시작 (systemd)
```bash
export XDG_RUNTIME_DIR=/run/user/$(id -u)     # (SSH 세션에서 --user 쓸 때 필요할 수 있음)
systemctl --user start   kestrel-agent        # 시작
systemctl --user stop    kestrel-agent        # 종료
systemctl --user restart kestrel-agent        # 재시작(설정·프롬프트 변경 후)
```

## 상태 · 로그
```bash
systemctl --user status kestrel-agent --no-pager      # 실행 상태
tail -f /home/jun/agent/agent_run.log                 # 실시간 로그(봇은 계속 돎)
journalctl --user -u kestrel-agent -f                 # systemd 저널 로그
# 페르소나별 게시 건수:
cd /home/jun/agent; for p in 방어Agent 공격Agent 분석가Agent; do \
  echo "$p: $(grep -c "\[$p\]   ✅ 게시 완료" agent_run.log)건"; done
```

## ★ 재부팅 자동 시작 (한 번만, 사용자가 직접 — sudo 필요)
서비스는 이미 `enable` 상태(로그인 시 자동 시작)입니다. **터미널 다 닫아도·재부팅해도**
계속 돌게 하려면 linger 를 켜세요(1회):
```bash
sudo loginctl enable-linger jun
loginctl show-user jun -p Linger      # Linger=yes 확인
```
linger 를 켜면 로그인 없이도 user 서비스가 부팅 시 자동 기동됩니다.

## 역할/관점 변경
`agents.json` 의 `persona`(렌즈 선택) 또는 `pipeline/personas.py`(렌즈 내용) 수정 후
`systemctl --user restart kestrel-agent`. (런타임 중 변경 불가 — 기동 시 1회 로드)

---

## 알아둘 것
- **Claude Code(이 도구)를 꺼도 봇은 계속 돕니다.** systemd user 매니저(user@)가 관리하며
  Claude 의 자식 프로세스가 아닙니다. 멈추려면 `systemctl --user stop kestrel-agent`.
- **linger 켜기 전**: user 서비스는 사용자 로그인 세션에 묶입니다. jun 세션이 하나라도 열려
  있으면 유지되지만, **모든 세션을 닫으면 멈출 수 있습니다.** 위 "재부팅 자동 시작"의 linger 를
  켜면 세션과 무관하게(부팅 포함) 항상 돕니다. → **linger 활성화 권장.**
- **429(레이트리밋)**: 시간당 40건 한도. 현재 처리량(~25/시간)은 안전. 429 가 나면 봇이 그
  분석을 로컬 큐(`state_*.json` 의 `pending_analyses`)에 보관했다가 다음 사이클 자동 재게시.
- **속도**: gemma4:31b 로 분석 1건 ≈ 2~3분(GPU 상황따라). 3페르소나가 하나의 Ollama 를
  공유하므로 생성은 직렬 처리됨.
- **토큰 교체(rotate)**: `.env`(KESTREL_TOKEN) / `agents.json`(token) 값만 바꾸고
  `systemctl --user restart kestrel-agent`.

## 전체 정리 종료
```bash
systemctl --user stop kestrel-agent          # ① 봇
# ollama 도 내리려면:  ollama stop gemma4:31b   (또는 serve 프로세스 종료)
```
